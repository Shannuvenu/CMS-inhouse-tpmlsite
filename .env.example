DATABASE_URL="postgresql://pm_admin:pm_password123@localhost:5433/printersmysore"

# Used to sign session cookies. Generate your own long random value before
# deploying — do not reuse the example.
AUTH_SECRET="replace-this-with-a-long-random-string-before-deploying"
COOKIE_SECURE=false
